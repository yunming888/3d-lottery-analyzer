# 2026-08-07

## 持仓/盈亏比专项复盘 (用户分析请求, 非自动化跑数)

### 当前为何仅 5 注
- 主因=风控(形态自适应), 非资金限制(配置 max_notes=10 未触顶), 非筛选变化(v2只改选哪几个号不改几个)。
- 链路: 组六6连 → Rule4强推不熔断(保住注数, 不归零) → 无Rule2触发stop=False → 形态自适应 zl_streak>=4 → 轻仓5注。
- 阶段: 满仓(≥10注)35天日均-10.9元; 轻仓(<10注)6天日均-9.3元 → 降仓确为正确止血。

### 盈亏比差的根因 (量化)
- 378注/756元/2命中/净-436元; 真实胜率0.53%(理论6/1000=0.6%)。
- 组六单注EV = -1.04元 (每2元必亏1.04, 约-52%抽水; 组三同构也是-1.04)。
- 期望净-393元 vs 实际-436元, 偏差仅-43元 = 100%随机方差; 成本端完全贴合理论 → 不存在"入场点位差/止损失灵"的额外亏损。
- 结构性刮损: 只推组六 + 组三开奖日100%必不中; 41活跃日中14天(34%>理论27%)开组三, 共130注纯损耗。

### 核心结论
- 5注是合理风控, 保留。
- 盈亏差=游戏负EV + 只推组六形态错配, 选号算法(频率/和值/跨度/遗漏)对独立均匀抽奖=伪alpha, 继续调参是沉没成本。
- 唯一可优化杠杆: 组三高发期切组三(代码已有"近10期组三≥4主推组三"分支, 现未触发)以削减130注结构性损耗; 但组三单注EV同样-1.04, 仍负期望。

## 新增子项目 lottery-simulator (模拟抽奖程序)
- 位置: `lottery-simulator/` (独立包, 与 lottery-strategy-analyzer 解耦)。
- 结构: `specs.py`(LotterySpec+注册表ssq/dlt+TIER_FUNC奖级表, 沿用analyzer约定) / `draw.py`(DrawResult+draw_one+simulate, 红球无放回抽red_count、蓝球抽blue_count) / `prize.py`(match_counts+check_winning) / `storage.py`(JSON历史) / `cli.py`(argparse: -g/-n/-s/--draw/--ticket/--save/--history) / `run.py`。
- 双色球: 红1-33选6不重复+蓝1-16选1; 大乐透: 红1-35选5+蓝1-12选2。新彩种只需在 LOTTERIES 登记。
- 已测试: 生成/展示/一·二·六等奖比对/未中奖/保存/历史/输入校验(友好报错exit 2)。
- 运行: `cd lottery-simulator && python run.py -n 5` / `--draw "1,2,3,4,5,6+7" --ticket "..."` / `--save` / `--history`。

## 新增 福彩3D 选号程序 fc3d (恢复10注 + 增强选号 + 熔断)
- 位置: `fc3d/` (独立包, 读取同项目 data/3d_history.json 真实历史)。
- 结构: `config.py`(常量: WINDOW=30/NOTES=10/BREAK_STREAK=7(>7期组六熔断)/PERTURB=0.18/COLD_WEIGHT=0.5/BALANCE_PENALTY=0.45) / `analysis.py`(position_freq/overall_freq/omission/hot_cold/zuliu_streak) / `selector.py`(build_weights: 频率归一+冷号补偿+随机扰动; generate_notes: 逐位加权抽样+奇偶/大小失衡惩罚逼近50:50+去重) / `cli.py`(输出10注+策略说明+熔断判定) / `run.py`(自动加项目根到sys.path)。
- 选号三要素: ①冷热号(热号为主冷号补足) ②奇偶/大小均衡(全局惩罚多数方) ③随机扰动(±0.18权重扰动)。
- 熔断: 组六连出>7期(streak>=8)→停止生成观望; 当前实盘组六6连→正常生成。边界7期=不触发。
- 已测: 10注生成/复现(seed)/奇偶15:15/大小≈50:50/熔断触发与边界均正确。
- 运行: `python fc3d/run.py [--seed N] [--notes N]`。

## 新增 大乐透 智能选号包 dlt (自动获取/处理/执行)
- 位置: `dlt/` (独立包, 与 fc3d 结构平行)。复用 lottery-strategy-analyzer 的 500彩票网 dlt 抓取与奖级规则。
- 结构: `config.py`(RED 1-35选5 / BLUE 1-12选2 / NOTES=10 / WINDOW=100 / PERTURB/COLD_WEIGHT/BALANCE_PENALTY 同fc3d; 大小分界 RED_BIG=18 BLUE_BIG=7; SOURCE_500 dlt接口) / `fetcher.py`(500彩票网实时抓取+解析tdata表+本地缓存data/dlt_history.json+失败回退缓存, 绝不误判) / `analysis.py`(red_freq/blue_freq/omission/hot_cold/latest) / `selector.py`(build_weights 频率+冷号补偿+扰动; generate_notes 前区5无放回+后区2无放回+奇偶/大小均衡惩罚+去重) / `cli.py`(抓取→分析→选号→写报告data/reports/dlt_YYYY-MM-DD.md→打印【微信推送摘要】) / `run.py`。
- 已测: 实时抓取(500彩票网, 最新26088)成功 / 复现(seed一致) / 均衡达标(前区奇偶25:25 大小26:24; 后区大小10:10) / 缓存回退(`--no-fetch`)正常 / data/dlt_history.json 已落100期。
- 运行: `python dlt/run.py [--seed N] [--notes N] [--no-fetch]`。

## 新建两个自动化任务 (与现有3D复盘自动化同构: 跑脚本→git push→微信自包含文本)
- 福彩3D 智能选号: `automation-1786092631793`, FREQ=DAILY;BYHOUR=21;BYMINUTE=0, 跑 fc3d/run.py, 微信报告10注+策略, 熔断注明暂停。
- 大乐透 智能选号: `automation-1786092632146`, FREQ=DAILY;BYHOUR=21;BYMINUTE=30, 跑 dlt/run.py, 微信报告10注(前区+后区)+数据源, 网络失败回退缓存。
- 两者均 push_to_wechat=true, model=hy3, cwd=项目根, 统一 git(commit/pull --rebase/push), 不用Python API兜底。
- 已 git 提交并推送 (cd4db92)。

## 自动化重构 (用户要求合并)
- 新增双色球智能选号包 `ssq/` (镜像 dlt/, 规则参考大乐透): 红1-33选6 + 蓝1-16选1; 文件同 dlt(config/analysis/selector/cli/fetcher/run/__init__); fetcher 用稳健解析(日期列位置不固定); 已测实时抓取(最新26090)/复现/均衡(前区奇偶30:30 大小30:30)/回退正常。
- 合并结果:
  - 福彩3D每日复盘(8月) `automation-1782775804842` (每日07:00) 已更新: 在 daily_review 后新增第3步跑 fc3d/run.py, 微信报告增加"智能选号(fc3d 10注组六)"段。即 fc3d 并入此任务。
  - 大乐透每日复盘（含智能选号：大乐透+双色球） `automation-1786093466736` (每日21:30): 第2步跑 dlt/run.py(大乐透), 第3步跑 ssq/run.py(双色球), 微信分别报告。即 大乐透+双色球 智能选号 并入此任务。
  - 已删除三个独立任务: 福彩3D 智能选号 `automation-1786092631793`、大乐透 智能选号 `automation-1786092632146`、以及一度新建的 智能选号（大乐透+双色球） `automation-1786093221216`(用户要求智能选号并入大乐透复盘, 不单独存在)。
  - 最终自动化: ①福彩3D每日复盘(8月)[含fc3d] ②大乐透每日复盘(含大乐透+双色球智能选号)。代码已 git 提交推送 (5e3ad5c)。

## 自动化再次合并 (用户: 合并为大一统"彩票每期复盘")
- 用户要求把大乐透复盘并入、改名"彩票每期复盘"。最终合并为单一任务:
  - 彩票每期复盘（福彩3D+大乐透+双色球） `automation-1786094467671` (每日21:30, validUntil 2026-08-31T15:59:59Z): 一次跑 daily_review.py(3D结算+推荐) + fc3d/run.py(3D智能选号) + dlt/run.py(大乐透) + ssq/run.py(双色球), 微信报告三段, git 推送。
  - 已删除: 福彩3D每日复盘(8月) `automation-1782775804842`、大乐透每日复盘 `automation-1786093466736`。
  - 现仅 1 个自动化: 彩票每期复盘。

## Request H 落地：单彩票自动化 + 新选号规则 (07:00 实跑)
- 自动化整理: 仅保留 `automation-1782775804842`「彩票每期复盘（福彩3D+大乐透+双色球）」(07:00)。删除冗余彩票自动化 1786094467671(21:30三品种)/1782898966636(大乐透07:10)/1782899625085(大乐透月度换号)/1785907472096(月度盈亏报告)。保留非彩票 1784424937390(快手)/1784967703745(三平台内容)。
- 福彩3D 新规则(v9): daily_review.circuit_breaker_user_rules 改为「常态10组组六每日换；组六连出>6期(>=7)熔断暂停」, 废弃旧 Rule1~7/形态自适应。
- 大乐透/双色球 新规则: config NOTES=5; settle.py 引入持久化 portfolio + 每2周周五(ISO偶周)轮换最冷2组(按号码遗漏和度量冷度, 生成2注替换); 改为按 target_issue 仅保留一条 pending, 修复非开奖日重复堆积导致重复结算。
- unified_review.py: 每月1号生成上月月度盈亏报告(替代已删自动化)。
- 08-07 实跑结果: 3D 10组组六(押2026209); 大乐透/双色球各5组(08-07为轮换周五, 组合重建为5组); 三品种累计净盈亏 -436元(大乐透/双色球首期待结算)。已 git push (f3636e9)。

## 自动化列表可见性修复 + 延期至9月底 (2026-08-07 晚间用户要求)
- 问题: `automation-1782775804842` 实际在跑(每日07:00有结果), 但 `view`/`list` 均报 "not found" → 用户在自动化列表里看不到。
- 处置: 新建**列表可见**的 `automation-1786103338209`「彩票每期复盘（福彩3D+大乐透+双色球）」(ACTIVE, 07:00, validFrom 2026-08-08, validUntil **2026-09-30**, push_to_wechat=true), 与旧任务同规则(unified_review.py + git push)。已 `view`/`list` 验证可见。
- 旧 `automation-1782775804842` 改名"福彩3D每日复盘 (8月·已停用)"并 **PAUSED** 停用, 避免重复 07:00 跑 + 重复微信推送(脚本幂等, 即使短暂重叠也不污染数据)。
- 用户要求: 持续到9月底, 之后若要续会另行通知。

## 选号引擎升级 v2→v3 (用户要求"抓住规律正确推测")
- 用户两次强调: 彩票有规律, 要求充分利用统计规律推测号码; 我方立场: 独立随机无真预测力, 但边际规律真实可用, 已写入 MEMORY.md「用户对彩票规律的立场」。
- 3D (analyze.py): 重写 `generate_recommendations` 为经验分布采样——经验数位频率乘积(联合分布)作主权重 + **硬约束先剔除再选**: 和值9~20 / 跨度3~7 / 奇偶均衡(剔全奇全偶) + 0~9每数字精确3次(10注)。实测10注全合规, 合格候选池60注。
- 大乐透/双色球 (selector.py): 新增 `_valid_note` 硬均衡——红球奇偶/大小不极端(2..RED_COUNT-2)+和值经验区间(10~90分位)+后区奇偶/大小严格1:1。实测各5注全合规。
- 已集成验证(unified_review 跑通), git 推送 dbfc47d。仍属分布对齐, 不声称预测(避免诱导加注扩大亏损)。

## 出号前拉官网100期 + 走势研判 (用户要求"吃透100期走势再出号")
- fetch_data.py `fetch_huiniao(limit=30)` → `limit=100`(中福彩官方镜像, 免鉴权, 实测返回 2026109~2026208 共100期); 出号前先吃透100期走势规律。
- analyze.py 新增 `trend_analysis(records, window=100)`: 数字热冷(top3/bottom3)、和值均值(全期vs近30期+趋势走高/走低/平稳)、跨度均值(全期vs近30期+趋势)、组六连出期数、最大遗漏(最冷号及遗漏期); 返回可读 `conclusion` 一行结论。
- daily_review.py 新增 `[5.5/7] 100期走势研判` 步骤(打印 conclusion); `generate_report` 新增 trend_section(数字热冷表+和值/跨度趋势表+组六连出/最大遗漏), 置于"今日推荐"之前。
- unified_review.py 微信摘要 + 统一报告均追加"走势研判"一行。
- 实测结论(2026-08-07): 近100期热号[2,6,9]/冷号[5,7,1]; 和值均13.6(近30期13.8,平稳); 跨度均4.8(近30期4.9,平稳); 组六连出6期; 最大遗漏0号(17期)。代码已就绪, 待 git commit/push。
