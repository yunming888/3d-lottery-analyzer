# 2026-08-29 工作日志

## 07:00 三品种统一复盘（automation-1786103338209）
- `unified_review.py` 执行成功，输出 `data/reports/unified_2026-08-29.md` + 3D/dlt/ssq 分报告。
- 福彩3D：昨日 2026230=066 组三，0命中 -20元；累计 -696元（3命中/588注）；组三4连/组六0连，未达7期熔断 → 推10注组六押 2026231。
- 大乐透：08-28 无开奖，最新 26097，累计 -100元；推5注押 26098。
- 双色球：08-28 无开奖，最新 26099，累计 -85元；推5注押 26100。
- 合计累计净盈亏 -881元。
- git 提交推送成功 (5e30a8b..ea8524f)。

## 15:50 出号表述整改（用户定：推号但标注为机选）
- 背景：用户提出"改变逻辑"，就"每日推号 vs 否认可预测"的表述冲突给出四选项，用户选「推号但标注为机选」。
- 改动范围（只改文案/标注，不动选号算法与结算）：
  - `analyze.py`：熔断 reason "正常推荐"→"正常出号"；CLI 打印 `[推荐号码]`→`[随机采样号码·等同机选]`。
  - `daily_review.py`：报告第四节标题 → `## 四、今日随机采样 (N注xx)｜等同机选·无预测力【选号引擎 v4】`；报告顶部 + 该节顶部新增性质声明；表名 `推荐数`→`投注数`、`| 推荐 |`→`| 投注 |`、`命中推荐`→`命中号码`；docstring 补充出号性质说明。
  - `dlt/settle.py`、`ssq/settle.py`：第四节同理改为「今日随机采样」+ 声明；`本期选号奇偶/大小均衡`→`本期采样…`；print 改为 `随机采样(等同机选) N 注`。
  - `unified_review.py`：微信摘要三品种行改为 `今日随机采样：N注（押XXX期）｜等同机选·无预测力【选号引擎 vX】`；合计段新增两行警示（含"任何收费荐号均属诈骗，切勿转账"）；统一报告顶部加性质声明；`选号均衡`→`采样均衡`。
  - `fc3d/cli.py`、`settle_and_report.py`（遗留/一次性）同步改标注。
- 保留的内部字段名：`analyze.py` 中 `"推荐号码"` dict key（多处引用，改会破坏调用链），仅作内部键，不对外展示。
- 幂等重跑验证通过：盈亏数字不变（-696/-100/-85），仅文案变化；git 提交推送成功 (ea8524f..252a046)。

## 16:10 新增投注结构性价比分析（复式 / 胆拖）
- 用户要求评估：大乐透/双色球「复式」、福彩3D「胆拖」的性价比。
- 新增独立脚本 `bet_structure_analysis.py`（**不接入 unified_review 每日链路**，单独跑；不改选号/结算逻辑），
  输出 `data/reports/bet_structure_analysis.md`。
- 方法：组合数学精确枚举（超几何分布 + Vandermonde 展开算各奖级注数），非蒙特卡洛；
  再用 40 万次 MC 交叉验证（临时脚本跑完即删）。两者一致，且与官方口径吻合
  （双色球单注中奖率 6.709% vs 官方 1/14.9=6.71%；大乐透 6.670% vs 官方 1/15=6.67%）。
- 奖金表取自项目现有配置：DLT_PRIZE / SSQ_PRIZE（dlt|ssq settle.py）与 3D 组六160/组三320/豹子1040（daily_review.py）。
- **核心结论（可复用的定论）**：
  1. ROI 恒定不随结构变化：大乐透固定奖 ROI 恒 26.9%、双色球恒 24.3%、3D 组六恒 48.0%。期望线性性决定，无法绕过。
  2. 复式只改方差不改概率：一等奖概率 = 注数/总组合数，与同注数分散单式完全相同；
     但「至少中小奖概率」复式远低于分散单式（大乐透 7+2 复式 12.25% vs 21注分散 76.54%）。
  3. 3D 胆拖与分散单式**完全等价**（不止期望，连中奖概率都相同）：开奖3位、胆占1位、剩2位须都在拖码里，
     每期最多中1注、无叠加。胆拖价值纯在选号语义，不在概率。
  4. 三个坑：全包蓝球 6+16「100%中奖但100%亏」；复式提升的中奖率是统计幻觉；
     大乐透追加投注只作用于浮动奖，固定奖 ROI 从 26.9% 掉到 18.0%。
- 开发踩坑记录（避免重犯）：
  - 3D 胆拖测算曾全为 0%：拖码误取 `range(20, 20+n)` 超出 0-9。正确做法是拖码从 `range(10)` 中剔除胆码后取。
  - ssq_compound 蓝球分支写错：不中蓝时注数应为 `cj * B`（不是 `cj * (B-1)`）；中蓝时才是 `cj` 中 + `cj*(B-1)` 不中。
    校验手段：复式固定奖期望必须严格等于「注数 × 单注期望」，不等即实现有误。
- git 提交推送成功 (db223ec..f353b0d)。
