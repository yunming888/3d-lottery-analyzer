# 2026-08-20 工作日志

## 重写三品种选号逻辑（用户要求：连续多期未中，要求优化）
- 先写 `backtest_selectors.py` 真实回测（walk-forward，对照纯随机基线），拿到基线证据：
  - 3D 旧策略实际命中 6.58% ≈ 理论无条件 6.00% → 策略未压低中奖率，"长期不中"是公平随机正常波动。
  - **发现旧逻辑实质缺陷**：旧 `_eligible` 硬约束（和值9-20/跨度3-7/奇偶1-2）把 120 个组六集合砍到 60 个，而真实组六开奖 **46.75% 落在约束带外** → 近一半组六开奖日命中率被锁死 0%（期望不变、方差更大，是"感觉一直不中"的体验来源）。
  - 大乐透/双色球：策略 EV 与纯随机基线基本相等，选号不改期望。
- 重写内容（不改函数签名/接口，不碰结算/熔断/报告流程）：
  - `analyze.py`（3D v4）：去掉近30期热号 recency 权重（赌徒谬误）；改为全窗口长期经验数位边际分布随机加权采样 + 覆盖均衡（0-9 每数字2~4次）+ 典型区间软约束；种子按最新期号派生 → 同日确定、跨日自然变化。移除过严硬约束。
  - `dlt/selector.py` / `ssq/selector.py`：保留经验权重/奇偶大小均衡；下调 `COLD_WEIGHT` 0.5→0.15（冷号补偿=赌徒谬误，降为装饰性）；新增**覆盖最大化贪心**（同等有效优先引入未覆盖数字，降低全不中方差）。
  - 回测复跑：3D 7.89% ≈ 理论 6.0%（无回归）；DLT/SSQ EV 仍负且≈随机基线。
  - 隔离验证通过：3D 组六/组三 10注去重合法+覆盖均衡、DLT/SSQ 注数/奇偶/覆盖合规。
- git 提交推送成功：`083b16f..5ca0246`（commit "重写选号逻辑:去除假性规律+经验分布采样+覆盖均衡优化"）。
- 立场重申：向用户明确"改的是去除假性规律+降方差，不改变理论中奖率（负EV不变）"，避免诱导加注。
